---
page_title: "{{.Name}} ({{.Type}}) - {{.RenderedProviderName}}"
description: |-
  Manages one custom field value on a GitHub Projects V2 item.
---

# {{.Name}} ({{.Type}})

This resource manages one text, number, date, single-select, or iteration value for a Projects V2 item.

## Example Usage

{{ tffile "examples/resources/project_item_field_value/example_1.tf" }}

{{ .SchemaMarkdown | trimspace }}

## Import

Import uses the project, item, and field GraphQL node IDs separated by colons:

```shell
terraform import github_project_item_field_value.example PVT_example:PVTI_example:PVTF_example
```
